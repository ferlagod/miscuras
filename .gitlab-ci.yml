image: alpine:latest

pages:
  stage: deploy
  script:
    - echo "Desplegando la Política de Privacidad de Mis Curas"
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
